{
    "private": true,
    "scripts": {
        "docs:dev": "vuepress dev docs --host localhost --temp _site --cache _cache",
        "docs:build": "vuepress build docs"
    },
    "devDependencies": {
        "vuepress": "^1.4.0"
    }
}
